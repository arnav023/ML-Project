Note to run the dockerized webb app for all 3 datasets use the commands: docker build -t refixmatch-audio-webapp .
and docker run -p 5000:5000 refixmatch-audio-webapp
and make sure to load the models to the folder docker folder. The models could not be loaded already due to github size limitations
